//Combine Two Arrays Using the concat Method


let a = [1, 2, 3].concat([4, 5, 6]);
console.log(a);

//=> Add Elements to the End of an Array Using concat Instead of push

var arr = [1, 2, 3];
arr.push([4, 5, 6]);
 
console.log( arr.concat(8)); // or concat([8])