



const plusFive = function(baseNum){
    return function(x){
        return baseNUm + x;
        // or return (x) => baseNUm + x;
    }
     
}

 


//const makePlusFunction = baseNum => num => baseNum + num;